CRUD-JS
Um CRUD (Create, Read, Update, Delete) simples usando Node.js, Express e MariaDB para gerenciar dados de forma eficiente.
Ideal pra quem quer entender o fluxo básico de uma aplicação back-end com banco de dados relacional.

🚀 Funcionalidades
-Criar novos registros.
-Listar todos os registros.
-Atualizar dados existentes.
-Deletar registros.
-Comunicação com MariaDB via queries SQL.

🛠 Tecnologias
-Node.js
-Express
-MariaDB
-Body-Parser (middleware para requisições)

📦 Instalação
Clone este repositório:
git clone https://github.com/diogobssdev/CRUD-JS.git

Entre na pasta:
cd CRUD-JS

Instale as dependências:
npm install

Configure o banco de dados:
Crie um banco no MariaDB.
Ajuste as credenciais no arquivo server.js.

Rode o servidor:
node server.js
Acesse no navegador:
http://localhost:3000
